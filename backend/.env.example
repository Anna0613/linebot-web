# DEPRECATED: 此檔案為舊版樣板，請改用 backend/env.example；以下內容僅供參考，不建議使用

# 基本設定
PROJECT_NAME="LineBot-Web Backend"
VERSION="1.0.0"
DESCRIPTION="LineBot-Web 統一 API 後端服務"
DEBUG=true
ENVIRONMENT="development"

# 安全設定
SECRET_KEY="your-super-secret-key-change-in-production"
JWT_SECRET_KEY="your-jwt-secret-key"
JWT_ALGORITHM="HS256"
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# 資料庫設定
DATABASE_HOST="localhost"
DATABASE_PORT="5432"
DATABASE_NAME="linebot_web"
DATABASE_USER="your_db_user"
DATABASE_PASSWORD="your_db_password"
DATABASE_URL="postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}"

# CORS 設定
ALLOWED_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"]
ALLOWED_HOSTS=["localhost", "127.0.0.1"]

# Webhook 設定
WEBHOOK_DOMAIN="https://your-ngrok-url.ngrok-free.app"

# LINE Login 設定
LINE_CLIENT_ID="your_line_client_id"
LINE_CLIENT_SECRET="your_line_client_secret"
LINE_REDIRECT_URI="http://localhost:3000/auth/line/callback"

# Redis 設定
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=""
REDIS_URL="redis://localhost:6379/0"

# 郵件設定
MAIL_USERNAME="your_email@example.com"
MAIL_PASSWORD="your_email_password"
MAIL_FROM="your_email@example.com"
MAIL_PORT=587
MAIL_SERVER="smtp.gmail.com"
MAIL_TLS=true
MAIL_SSL=false

# MinIO 設定
MINIO_ENDPOINT="localhost:9000"
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"
MINIO_SECURE=false
MINIO_BUCKET_NAME="message-store"
MINIO_PUBLIC_URL="http://localhost:9000"

# AI / Google Gemini 設定
# 到 https://aistudio.google.com/ 取得 API Key
GEMINI_API_KEY=""
# 可選：gemini-1.5-flash 或 gemini-1.5-pro
GEMINI_MODEL="gemini-1.5-flash"
AI_MAX_HISTORY_MESSAGES=200
